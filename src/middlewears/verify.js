const jwt = require("jsonwebtoken");
const User = require("../models/user");

// const userAuth = (req, res, next) => {
//   try {
//     const { token } = req.cookies;
//     if()
//   } catch (err) {
//     res.status(400).send("Cannot get Details. Please login");
//   }
// };
